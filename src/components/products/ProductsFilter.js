import Select from "../UI/Select";

const ProductsFilter = () => {
  return (
    <div>
      <Select id="category" label="Category" name="category" options={[]} />
    </div>
  );
};

export default ProductsFilter;
